# Core API
fastapi>=0.109.0,<1.0.0
python-multipart>=0.0.9,<1.0.0
uvicorn>=0.27.0,<1.0.0
pydantic>=2.6.3,<3.0.0
pydantic-settings>=2.14.2,<3.0.0
python-dotenv>=1.0.1,<2.0.0
httpx>=0.27.0,<1.0.0
slowapi>=0.1.9,<1.0.0
cachetools>=7.1.4,<8.0.0
tenacity>=8.2.0,<10.0.0

# Database & Auth
supabase>=2.3.6,<3.0.0
bcrypt>=4.0.0,<5.0.0
PyJWT>=2.8.0,<3.0.0
requests>=2.34.2,<3.0.0
psycopg2-binary>=2.9.9,<3.0.0
psycopg[binary]>=3.1.0,<4.0.0
psycopg-pool>=3.2.0,<4.0.0

# Background jobs
APScheduler>=3.10.0,<4.0.0

# AI / LangChain
# NOTE(degrau-3-fase-2): core LLM congelado em 2.x/1.2.x. Subir langchain p/ >=1.3.9
# (fix do GHSA-gr75-jv2w-4656) ARRASTA langgraph-checkpoint p/ 4.x (langchain 1.3.x
# exige langchain-core>=1.4.7 -> checkpoint>=4.1). Esse major do grafo exige regressão
# completa do motor (booking/RAG/triage) e vira onda dedicada. Por isso:
#   - langchain pinado <1.3.0 (mantém checkpoint 2.x), GHSA em allowlist do pip-audit
#   - langgraph-checkpoint pinado <3.0.0 explícito (transitivo; sem pin saltaria p/ 4.x
#     num install fresco do CI/Render), 3 CVEs em allowlist do pip-audit
#   - langchain-openai cap mantido <1.0.0, PYSEC-2026-76 em allowlist do pip-audit
langgraph>=1.0.0,<2.0.0
langgraph-checkpoint>=2.1.0,<3.0.0
langgraph-checkpoint-postgres>=2.0.0,<3.0.0
langchain>=1.0.0,<1.3.0
langchain-openai>=0.3.0,<1.0.0
openai>=1.40.0,<2.0.0
pillow>=12.2.0,<13.0.0

# Linting & Testing
ruff>=0.15.20,<1.0.0
# NOTE(degrau-3-fase-2): pytest pinado <9 (CVE-2025-71176, dev-only). Subir p/ 9.x
# regride pytest-asyncio (0.26 exige pytest<9; o cap <1.0.0 cairia p/ 0.23.x), o que
# desestabiliza a suíte async. Bump do pytest+plugins vira onda dedicada; CVE em allowlist.
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
pytest-mock>=3.15.1,<4.0.0
pytest-cov>=4.1.0,<6.0.0

# Security tooling (SAST + supply-chain audit) — dev/CI deps
bandit>=1.7,<2.0
pip-audit>=2.7,<3.0
