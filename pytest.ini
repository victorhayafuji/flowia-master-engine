[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    agent_flow: multi-turn LangGraph flow tests (Chat Test simulator)
    adversarial: security/adversarial perimeter tests
    llm_behavior: optional live LLM behavior checks (RUN_LLM_BEHAVIOR_TESTS=1)
filterwarnings =
    ignore::DeprecationWarning
