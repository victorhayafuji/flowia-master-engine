# packages/auth_core

Config, tenant context, JWT, Supabase handler, dependencies, rate limiter.

- Router auth: `auth_router.py` (`/api/v1/auth/*`)
- Settings: `config.py` — `OPENAI_API_KEY`, `MODEL_NAME`, `VISION_MODEL_NAME`, `EMBEDDING_MODEL_NAME` — ver [`CLAUDE.md`](../../CLAUDE.md) §33
